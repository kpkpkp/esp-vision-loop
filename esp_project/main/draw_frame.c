#include "draw_api.h"

/* No-op placeholder. On-device compilation replaces this with the actual drawing.
 * The splash screen (in main.c) stays visible until draw_frame() draws over it. */
void draw_frame(void) {
}
